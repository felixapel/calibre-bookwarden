# No explicit CVE exceptions for the Caddy edge image.
# CI still applies its HIGH/CRITICAL and --ignore-unfixed scan policy.
# The builder and modules are patched; the exact-image CI scan is required
# evidence and must not be described as proof of zero vulnerabilities.
